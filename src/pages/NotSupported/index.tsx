import {View} from 'vin-react';

import styles from './.module.css';

const NotSupported: React.FC = () => {
  return <View className={styles['not-supported-page']}>Not Supported</View>;
};

export default NotSupported;
