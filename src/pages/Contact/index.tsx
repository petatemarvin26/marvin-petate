import {View} from 'vin-react';

import styles from './.module.css';

const ContactPage: React.FC = () => {
  return <View className={styles['contact-page']}>CONTACT</View>;
};

export default ContactPage;
